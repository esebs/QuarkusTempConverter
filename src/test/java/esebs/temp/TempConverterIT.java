package esebs.temp;

import io.quarkus.test.junit.QuarkusIntegrationTest;

@QuarkusIntegrationTest
public class TempConverterIT extends TempConverterTest {

    // Execute the same tests but in native mode.
}
